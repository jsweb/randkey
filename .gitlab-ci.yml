image: node:latest

cache:
    paths:
    - node_modules/

build:
    stage: build
    script:
        - yarn
        - yarn build

report:
    stage: test
    script:
        - yarn report

pages:
    stage: deploy
    only:
        - master
    script:
        - yarn deploy
    artifacts:
        paths:
            - public
